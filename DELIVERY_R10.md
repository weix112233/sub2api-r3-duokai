# Sub2API R10 Source Delivery

> 本文件保留 R10 源码交付阶段的历史事实。当前源码候选说明见 [RELEASE-R11.md](RELEASE-R11.md)。
> 下方旧运行态和测试数量不是当前部署状态，也不覆盖 R11。

## R10 交付源码

R10 版本说明以 [RELEASE-R10.md](RELEASE-R10.md) 为准。
源码已包含 `6288461` 的容量分类及跨通道错误处理修复，旧 R7 发布页不再是最新入口。
下方 R9 和早期 R10 数据保留其历史验证范围，不代表当前运行部署或当前产物验收。

## 后续防破限源码修复

本分支后续增加了防破限业务边界修复，详见 `delivery/ANTIBYPASS_REPAIR.md`。
下面的 R10 归属修复验证数据保留为历史记录，不能代替后续防破限验证；
新的验证结果记录在 `delivery/antibypass-repair-validation.json`。
原 `ace20e9` 下载包仍是原提交快照，不包含后续修复，不应与当前分支混用。

## 交付状态

这是源码交付版本，不是已经构建或已经部署的 R10 二进制发布。

- Git 基线：`d9e796f`，生产 R7 源码提交。
- 本次包含已经验证的 R9 修复、R10 归属持久化和 Redis 原子性修复、
  防破限边界修复，以及 `6288461` 的 Selected model is at capacity 分类修复。
- 在本源码交付阶段，本地生产进程为 `0.2.0-local-gpt6-production-audit-r9`。
- 当时尚未构建 R10 二进制或切换生产；这些历史描述不能覆盖后续真实部署记录。本说明、源码归档和测试结果均不构成部署准入许可。
- 不包含生产凭据、账号配置、数据库、运行日志、依赖缓存或应用二进制。

## 主要变更

1. 统一请求级容量错误重试预算，保留 HTTP、SSE 和 WebSocket 的错误语义。
2. 补齐 WebSocket 升级、首轮、终态写出及进程退出期间的排空管理。
3. 采用前端先构建、后端嵌入前端的构建顺序和显式版本元数据。
4. 支付 SDK 改为按需加载，更新依赖安全版本及对应验证。
5. 归属持久化脱离已结束 HTTP 请求的取消状态，但保持独立有限超时。
6. Redis 归属双键原子提交、成对读取；拒绝跨用户覆盖和不完整归属记录。
7. 生产与安装检查的 Redis 客户端启用 context 截止时间支持。

早期归属修复的九个后端文件曾与固定 R9 构建树比对；该历史比较不涵盖
后续防破限和容量修复，不能用于断言当前仅有九文件差异。
提交中的嵌入前端资源来自既有 R9 构建，本次源码交付没有重新执行应用构建。

## 验证

记录摘要在 `delivery/r10-validation.json`。这些是本地已有执行结果，不是本次 Git 推送产生的远端 CI 结果。

| 检查 | 结果 |
| --- | --- |
| 最终后端 tagged unit | 18,395 个通过测试事件，0 失败 |
| 归属、缓存及安装检查专项 race | 28 个通过测试事件，0 失败 |
| 生产 Redis ACL DRYRUN | 8 项通过；未执行实际写入 |
| R9 前端完整测试 | 1,846 项通过 |
| R9 前端 lint | 775 个文件，0 错误、0 警告 |
| R9 生产依赖审计 | 已记录的审计结果为 0 告警 |

## 构建入口

须先完成执行环境要求的正式准入、存储检查和适用审批；不要因本包存在而跳过这些步骤。
构建要求以 `backend/go.mod` 和前端锁文件为准。当前记录使用 Go 1.27.0 和 pnpm 9。
缓存、依赖及构建输出应放在该执行环境批准的本地工作位置。

在依赖安装和正式准入已满足的前提下，仓库已有入口为：

```sh
pnpm --dir frontend install --frozen-lockfile
make build VERSION=0.2.0-local-gpt6-production-audit-r10
```

输出为 `backend/bin/server`。此文件不包含在本源码交付包中。

## 上线与回滚边界

- 构建后绑定源码提交、二进制 SHA-256、版本、构建时间和验证结果。
- 生产切换前确认维护授权、进程身份、发布互斥和当前配置。
- 不允许依赖 R9 旧写入器与 R10 新写入器并存时的归属原子性。
- 后续 R10 回滚应回到已核验的 R9 二进制和 R9 启动绑定，不要误用更早的 pre-R9 启动脚本。
- Redis 超时仅限制调用方等待，不等于 Redis 已撤销提交；恢复时需要新上下文读回或同用户幂等重试。
- 正式切换后验证真实归属落盘、续接、取消、超时恢复、错误、延迟、资源及计量。
- 现有短时测试不能证明长期 SLO、峰值容量、供应商现金成本或全部生产功能已经达标。

本次提交使用 `[skip ci]`，用于交付本地已验证的不可变源码，不触发远端构建或自动发布。
